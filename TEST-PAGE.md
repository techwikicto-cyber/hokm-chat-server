# صفحه تست چت (Chat Test Page)

یک صفحه ساده برای بررسی دستی و خودکار فانکشنالیتی `hokm-chat-server`.
دو کلاینت مستقل روی یک صفحه باز می‌شوند تا پخش پیام (broadcast) بین کاربرها قابل مشاهده باشد.

## اجرا

صفحه به صورت پیش‌فرض **غیرفعال** است و فقط با متغیر محیطی زیر فعال می‌شود:

```bash
ENABLE_TEST_PAGE=true \
TEST_PAGE_TOKEN=یک-رشته-تصادفی-طولانی \
LISTEN_PORT=3008 \
node simple-chat-server.js
```

یا با pm2 (به `ecosystem.config.js` اضافه کنید یا موقتی):

```bash
ENABLE_TEST_PAGE=true TEST_PAGE_TOKEN=xxxx pm2 start ecosystem.config.js --update-env
```

سپس در مرورگر:

```
http://<server-ip>:3008/test-chat?token=xxxx
```

اگر `TEST_PAGE_TOKEN` را ست نکنید صفحه بدون توکن باز می‌شود؛ **روی سرور عمومی حتماً توکن بگذارید**،
چون هر کسی که به این مسیر دسترسی داشته باشد می‌تواند پیام رمزشده معتبر بسازد.

## پیش‌نیازها

- **MySQL** با جدول‌های `public_chat` و `private_chat` (برای تاریخچه چت)
- **Redis** روی `REDIS_IP:REDIS_PORT` — اگر Redis بالا نباشد، `send_chat_message` به‌طور کامل
  از کار می‌افتد (کنترل ضدفلود قبل از ارسال پیام به Redis نیاز دارد).
- اگر سرور در حالت **cluster** پی‌ام‌۲ با چند instance اجرا می‌شود، `SHOULD_USE_NGINX_REDIS=true`
  لازم است؛ وگرنه دو کلاینت ممکن است روی دو worker مختلف بیفتند و پیام هم را نبینند.
  صفحه تست این حالت را تشخیص می‌دهد و هشدار می‌دهد.

## چه چیزی تست می‌شود

| # | تست | چه چیزی را ثابت می‌کند |
|---|-----|------------------------|
| ۱ | اتصال + `get_user_details` | هندشیک سوکت، رمزنگاری AES، بارگذاری تاریخچه عمومی |
| ۲ | چت عمومی A → B | broadcast اتاق عمومی و ساختار `sender_details` |
| ۳ | تاریخچه عمومی | `INSERT` و `SELECT` جدول `public_chat` |
| ۴ | اتاق خصوصی | `join_chat_room` و broadcast اتاق خصوصی |
| ۵ | تاریخچه خصوصی | `INSERT`/`SELECT` جدول `private_chat` |
| ۶ | ضدفلود | کول‌داون ۸ ثانیه‌ای روی Redis (`SETEX`/`GET`) |
| ۷ | فیلترها | حذف شماره موبایل و کاراکترهای خاص از متن پیام |
| ۸ | ارسال بدون احراز هویت | سرور باید `request_user_details` برگرداند |
| ۹ | ورودی خراب | پیام رمزنشده نباید سرور را بیندازد |

دکمه «اجرای همه تست‌ها» همه را پشت سر هم اجرا می‌کند و نتیجه هر کدام را با علت شکست نشان می‌دهد.
برای تست دستی هم می‌توانید در هر پنل مستقیماً `get_user_details` / `join_chat_room` / `send_chat_message`
بفرستید و لاگ رمزگشایی‌شده هر دو طرف را ببینید.

## نکات

- سرور پیام را با `socket.to(room)` پخش می‌کند، پس **فرستنده پیام خودش را دریافت نمی‌کند**.
  برای همین دو کلاینت لازم است.
- کول‌داون ضدفلود بر اساس `user_id` است. تست‌های خودکار قبل از هر ارسال یک `user_id` تصادفی
  برای کلاینت A می‌سازند تا در کول‌داون گیر نکنند. در تست دستی اگر پیام دوم رد شد، ۸ ثانیه صبر کنید.
- نام اتاق خصوصی اگر عددی و کوچک‌تر از `10000000` باشد، سرور عمداً آن را در دیتابیس ذخیره نمی‌کند.
- کلید AES هرگز به مرورگر فرستاده نمی‌شود؛ رمزگذاری/رمزگشایی از طریق
  `POST /test-chat/encrypt` و `POST /test-chat/decrypt` روی خود سرور انجام می‌شود.

## حذف از پروداکشن

کافی است `ENABLE_TEST_PAGE` را ست نکنید — هیچ مسیری رجیستر نمی‌شود و هیچ فایلی سرو نمی‌شود.
